// swift-tools-version:5.3
import PackageDescription

let package = Package(
    name: "Email",
    platforms: [.macOS(.v11)],
    dependencies: [],
    targets: [
        .target(
            name: "Email",
            dependencies: []),
    ]
)
